{
  "name": "realblock-test",
  "version": "1.0.0",
  "description": "testing the full cycle of signing up a new user",
  "scripts": {
    "cypress:test-headless": "cypress run --browser chrome --headless",
    "cypress:gui": "cypress run --browser chrome --no-exit"
  },
  "author": "Fadi Razouk",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^6.8.0"
  },
  "dependencies": {
    "cypress-file-upload": "^5.0.3"
  }
}
